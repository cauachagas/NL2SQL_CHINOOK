[project]
name = "nl2sql_chinook"
version = "0.1.0"
description = "A project to convert natural language to SQL queries"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0.47",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
